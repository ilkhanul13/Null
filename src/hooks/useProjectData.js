import { projects } from '../utils/projectsData';

const useProjectData = () => {
  // Di sini bisa ditambahkan logika filter atau sorting jika perlu
  return { projects };
};

export default useProjectData;